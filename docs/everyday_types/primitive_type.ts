/**
 * # 원시 타입 (Primitive Types)
 */

// 문자열(string) 타입 예제
const message:string = "Hello, TS";
console.log(message);

// 숫자(number) 타입 예제
const count: number = 42;
console.log(count);

// 불리언(boolean) 타입 예제
const isDone: boolean = false;
console.log(isDone);

